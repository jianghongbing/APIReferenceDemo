//: Playground - noun: a place where people can play

import UIKit
//Bool:布尔类型来表示true和false,通常用于条件判断
let isTrue = true
let isFlase = false
if isTrue {
    print("it is true")
}else {
    print("it is false")
}
//在Swift中,不能用某个不等于0的数字和不能nil的对象来作为Bool类型的变量来使用
//if 1000 {
//    print("it is true")
//}

//Bool类型之间的逻辑运算
//1.&&逻辑与,同时为true,最后的结果才为true,否则为flase,如果&&前面为false,将不会对&&后面的表达式做运算,直接返回false
//2.||逻辑或,如果其中有一个表达式的值为true,最后的结果就为true,如果||前面为true,将不会对&&后面的表达式做运算,直接返回true
//3.!逻辑非,如果后面的表达式的值为true,结果为false,如果后面的表达式的值为false,结果为true







